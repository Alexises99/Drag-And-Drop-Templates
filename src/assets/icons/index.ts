import leftIcon from './left.svg'
import centerIcon from './center.svg'
import rightIcon from './right.svg'
import type { Alignment } from '@types'

export const aligmentRecord: Record<Alignment, string> = {
  left: leftIcon,
  center: centerIcon,
  right: rightIcon
}
